import React from 'react';
import { styled } from '@mui/material/styles';
import { Typography, Box, Grid } from "@mui/material";

export const InicioContainer = styled(Box)(({ theme }) => ({
  display: 'flex',
  justifyContent: "space-around",
  //background: 'red',
  width:'99%',
  height: "85%",
  alignItems: "center",
  margin:'auto',

}));

